{
  "private": true,
  "scripts": {
    "clean": "lerna clean && rm -rf node_modules",
    "lint": "eslint --fix \"packages/**/*.js\"",
    "release": "yarn lint && yarn test && lerna publish",
    "test": "spraoi-test --coverage"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@spraoi/eslint-config": "^0.0.7",
    "@spraoi/test": "^0.0.7",
    "eslint": "^5.9.0",
    "lerna": "^3.4.3",
    "prettier": "^1.15.2",
    "prop-types": "^15.6.2",
    "react": "^16.6.3",
    "react-dom": "^16.6.3"
  }
}
