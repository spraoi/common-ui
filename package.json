{
  "private": true,
  "scripts": {
    "lint": "eslint --fix \"**/*.js\"",
    "release": "yarn lint && yarn test && lerna publish",
    "test": "lerna run test --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@spraoi/eslint-config": "^0.0.1",
    "eslint": "^5.9.0",
    "lerna": "^3.4.3",
    "prettier": "^1.15.2"
  }
}
