{
  "private": true,
  "name": "common-ui",
  "scripts": {
    "clean": "lerna clean && rm -rf node_modules",
    "format": "prettier --write --ignore-path .gitignore \"**/*.{js,json,css,scss,md,yml}\"",
    "lint": "yarn lint:js && yarn lint:styles",
    "lint:js": "eslint --ignore-path .gitignore \"packages/**/*.js\"",
    "lint:styles": "stylelint --ignore-path .gitignore \"packages/**/*.js\"",
    "release": "yarn lint && yarn test && lerna publish",
    "report-coverage": "cat coverage/lcov.info | coveralls",
    "test": "jest --coverage"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@babel/core": "^7.1.6",
    "@babel/plugin-transform-runtime": "^7.1.0",
    "@babel/runtime": "^7.1.5",
    "@spraoi/babel-preset": "^0.12.0",
    "@spraoi/eslint-config": "^0.12.0",
    "@spraoi/jest-config": "^0.12.0",
    "@spraoi/stylelint-config": "^0.12.0",
    "aws-amplify": "^1.1.10",
    "babel-core": "^7.0.0-bridge",
    "babel-plugin-styled-components": "^1.9.0",
    "babel-preset-gatsby": "^0.1.4",
    "coveralls": "^3.0.2",
    "eslint": "^5.9.0",
    "gatsby": "^2.0.55",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "prettier": "^1.15.2",
    "prop-types": "^15.6.2",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-helmet": "^5.2.0",
    "styled-components": "^4.1.1",
    "stylelint": "^9.8.0"
  }
}
